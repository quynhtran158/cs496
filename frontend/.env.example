# ── Backend API URL ────────────────────────────────────────────────────────────
# Set this to your Render backend URL once deployed (no trailing slash)
# During local dev this defaults to http://localhost:5001 (see src/lib/api.ts)
VITE_API_URL=https://your-backend-name.onrender.com
